# Frontend Simulator

Single-page vanilla frontend simulator for SSMA protocol testing.

## Run

Use any static server from repo root. Example:

```bash
python3 -m http.server 8080
```

Then open:

`http://127.0.0.1:8080/examples/frontend-sim/index.html`
