# SSMA Template Catalog

This folder is the template discovery root for `csma-ssma-cli`.

## Available Templates

- `js-gateway` (`templateId: ssma-js-gateway`)
- `rust-gateway` (`templateId: ssma-rust-gateway`)

## Validation

Run:

```bash
npm run validate:templates
```
